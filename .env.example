# Deixe vazio em dev: o proxy do Vite (vite.config.js) já encaminha as
# chamadas para o backend em localhost:8000.
# Em produção, se o frontend não estiver atrás do mesmo reverse-proxy do
# backend, aponte para a URL pública da API:
VITE_API_URL=
